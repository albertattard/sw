entries:
  - type: Command
    commands: |
      printf 'main\n' >> cleanup-scalar.txt
    cleanup: |
      NAME='cleanup context'
      if [ -n "$NAME" ]; then
        printf '%s\n' "$NAME" >> cleanup-scalar.txt
      fi
